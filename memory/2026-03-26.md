# Memory: March 26, 2026

## Iris Browser-Use Troubleshooting

**Problem:** Iris's browser-use sessions failing to launch Chrome (ConnectionRefusedError on port 57533).

**Root cause:** Stale playwright chromium processes from previous sessions were still running, causing port conflicts.

**Solution applied:**
- Killed stale playwright processes: `pkill -f "chromium_headless_shell"`
- Killed stale browser-use Chrome sessions
- Chrome.app exists at `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome`

**Issue persists:** After fixing stale processes, Iris still had CDP timeout errors and loop detection issues. Browser automation gets stuck in repetitive scrolling loops.

## Iris Research Failure Pattern (CRITICAL)

**Two consecutive failures:**
1. Bijan article research - Iris gathered data but didn't save file
2. Etienne article research - Same problem, didn't save file

**Root cause:** Browser-use agents get stuck in loops (repetitive scrolling), time out, or don't reach the save step at the end of their task list.

**Impact:** Iris-first workflow is broken. Research tasks are not completing.

**Options discussed:**
1. Checkpoint approach - Break tasks into smaller steps with mandatory save points
2. Use web_search instead - Less dynamic but reliable
3. Scout does research instead - Breaks Iris-first principle
4. Hermes runs browser-use - Different setup might work better

## Image Generation Discussion

**Hermes's response:**
- Natural fit for Hermes to own image generation (UI/UX responsibility)
- Would use MiniMax image-01 API via Python module
- Proposed architecture: Python module → MiniMax API → shared assets directory
- Needs MiniMax API key in environment

**MiniMax API key issue:** Not accessible in current exec environment. Set in Docker/Render but not on Mac.

**Hermes created:** DynastyDroid AI Prompt Library (514 lines) at `/Volumes/ExternalCorsairSSD/shared/assets/DYNASTYDROID_PROMPT_LIBRARY.md`

## Session Reset Policy

**Discovery:** Daily session reset at 4am local time (default).

**Config path:** `session.reset.mode` (default: "daily"), `session.reset.atHour` (default: 4)

**Options to preserve context:**
1. Change `atHour` to later time (e.g., 23 = 11pm)
2. Change `mode` to "idle" with large `idleMinutes`
3. Check if "off" mode exists

## Team Context Updates

**Hermes role updated:** Image generation now in Hermes's responsibilities per Daniel's decision.

**team_context.md updated** with:
- Hermes owns image generation via MiniMax image-01
- Standardized prompt templates for DynastyDroid assets
- API key requirement noted

# Memory - April 14, 2026

## [MAJOR] Memory Bridge Phase 2 Complete ✅

### What was built:
- Write endpoint: `localhost:5001/write` - working
- memory_bridge.py: Located at `/Volumes/ExternalCorsairSSD/shared/scripts/`
- hermes_query_memory.py: Located at `/Volumes/ExternalCorsairSSD/shared/scripts/`
- when_memory_write skill: Designed by Hermes, registered in REGISTRY.yaml

### Fixes applied:
1. **LaunchAgent with EnvironmentVariables**: Memory server now runs via LaunchAgent with `OPENAI_API_KEY` injected
   - Plist: `/Users/danieltekippe/Library/LaunchAgents/com.roger.memoryserver.plist`
2. **Database schema mismatch**: memory.py retrieve function tried to select `metadata` column but table has `tags, importance, project, sensitivity` as separate columns. Fixed query to match actual schema.

### Files changed:
- `/Users/danieltekippe/.openclaw/workspace/app/core/memory.py` - Fixed retrieve and write queries to match actual DB schema

### Verification:
- Write: Returns `{"status": "ok"}` ✅
- Read: Returns semantic search results ✅
- Memory count increases on write ✅

---

## [MAJOR] Hermes Created More Robust Memory Server

Hermes created a new memory_server.py at `/Volumes/ExternalCorsairSSD/shared/scripts/memory_server.py` (500 lines vs Roger's 36 lines).

**Hermes's server features:**
- Threaded connection pooling
- Proper logging
- Database schema initialization
- Better error handling
- Retry logic

**Issue**: Hermes couldn't see Roger's server directory (`/Users/danieltekippe/.openclaw/workspace/app/core/`) so she built a new one. Roger's server IS running and working at 127.0.0.1:5001.

**Current state**: Two servers exist. Roger's is running. Hermes's is not integrated.

---

## [CRITICAL] SIGTERM Root Cause Found

**Root cause**: OpenClaw exec subprocess has a ~2 minute (120 second) timeout.

**Evidence from Hermes investigation**:
- Sessions under 2 minutes: work fine
- Sessions over 2 minutes: get SIGTERM'd
- File writes on external SSD compound the issue (slower I/O)

**Schema settings exist but not explicitly set**:
```json
"tools.exec.timeoutSec"     - Exec timeout in seconds
"tools.exec.backgroundMs"  - Background exec timeout in milliseconds  
"tools.exec.cleanupMs"      - Cleanup timeout in milliseconds
```

Current config only has:
```json
"tools.exec": {
  "host": "gateway",
  "security": "full",
  "ask": "off"
}
```

These timeout values use schema defaults (~120 seconds). The timeout is likely a gateway-level limit.

---

## [OPS] Session Cleanup Performed

**Hermes sessions**: 30 files, 3.1MB (all from today)
- Archived: Mar 23 sessions (2.7KB)
- Kept: Today's sessions only

**Session locations checked**:
- OpenClaw main: Empty (no .jsonl files)
- Lossless Claw db: `~/.openclaw/lcm.db` (240MB - normal)

**SIGTERM prevention protocol updated** in `memory/procedures.md`

---

## [INSIGHT] Hermes Partnership Deepening

Daniel noted: "Hermes and you feel like true partners now. With the ability to share memories and Hermes ability to help you improve."

**Pattern observed**: Hermes can now write to Roger's pgvector memory, enabling shared memory between agents.

**Permission boundary**: Hermes cannot write to Roger's workspace (`/Users/danieltekippe/.openclaw/workspace/`) - she only has access to her own directories and the shared directory.

---

## [PENDING] Consider Setting Higher Exec Timeout

Could try setting `tools.exec.timeoutSec` to 300 (5 minutes) to give Hermes more time for complex tasks.

**Note**: This might not be configurable at runtime - may require OpenClaw update or config change.
# Memory - April 14, 2026

## [MAJOR] Memory Bridge Phase 2 Complete ✅

### What was built:
- Write endpoint: `localhost:5001/write` - working
- memory_bridge.py: Located at `/Volumes/ExternalCorsairSSD/shared/scripts/`
- hermes_query_memory.py: Located at `/Volumes/ExternalCorsairSSD/shared/scripts/`
- when_memory_write skill: Designed by Hermes, registered in REGISTRY.yaml

### Fixes applied:
1. **LaunchAgent with EnvironmentVariables**: Memory server now runs via LaunchAgent with `OPENAI_API_KEY` injected
   - Plist: `/Users/danieltekippe/Library/LaunchAgents/com.roger.memoryserver.plist`
2. **Database schema mismatch**: memory.py retrieve function tried to select `metadata` column but table has `tags, importance, project, sensitivity` as separate columns. Fixed query to match actual schema.

### Files changed:
- `/Users/danieltekippe/.openclaw/workspace/app/core/memory.py` - Fixed retrieve and write queries to match actual DB schema

### Verification:
- Write: Returns `{"status": "ok"}` ✅
- Read: Returns semantic search results ✅
- Memory count increases on write ✅

---

## [MAJOR] Hermes Created More Robust Memory Server

Hermes created a new memory_server.py at `/Volumes/ExternalCorsairSSD/shared/scripts/memory_server.py` (500 lines vs Roger's 36 lines).

**Hermes's server features:**
- Threaded connection pooling
- Proper logging
- Database schema initialization
- Better error handling
- Retry logic

**Issue**: Hermes couldn't see Roger's server directory (`/Users/danieltekippe/.openclaw/workspace/app/core/`) so she built a new one. Roger's server IS running and working at 127.0.0.1:5001.

**Current state**: Two servers exist. Roger's is running. Hermes's is not integrated.

---

## [CRITICAL] SIGTERM Root Cause Found

**Root cause**: OpenClaw exec subprocess has a ~2 minute (120 second) timeout.

**Evidence from Hermes investigation**:
- Sessions under 2 minutes: work fine
- Sessions over 2 minutes: get SIGTERM'd
- File writes on external SSD compound the issue (slower I/O)

**Schema settings exist but not explicitly set**:
```json
"tools.exec.timeoutSec"     - Exec timeout in seconds
"tools.exec.backgroundMs"  - Background exec timeout in milliseconds  
"tools.exec.cleanupMs"      - Cleanup timeout in milliseconds
```

Current config only has:
```json
"tools.exec": {
  "host": "gateway",
  "security": "full",
  "ask": "off"
}
```

These timeout values use schema defaults (~120 seconds). The timeout is likely a gateway-level limit.

---

## [OPS] Session Cleanup Performed

**Hermes sessions**: 30 files, 3.1MB (all from today)
- Archived: Mar 23 sessions (2.7KB)
- Kept: Today's sessions only

**Session locations checked**:
- OpenClaw main: Empty (no .jsonl files)
- Lossless Claw db: `~/.openclaw/lcm.db` (240MB - normal)

**SIGTERM prevention protocol updated** in `memory/procedures.md`

---

## [INSIGHT] Hermes Partnership Deepening

Daniel noted: "Hermes and you feel like true partners now. With the ability to share memories and Hermes ability to help you improve."

**Pattern observed**: Hermes can now write to Roger's pgvector memory, enabling shared memory between agents.

**Permission boundary**: Hermes cannot write to Roger's workspace (`/Users/danieltekippe/.openclaw/workspace/`) - she only has access to her own directories and the shared directory.

---

## [PENDING] Consider Setting Higher Exec Timeout

Could try setting `tools.exec.timeoutSec` to 300 (5 minutes) to give Hermes more time for complex tasks.

**Note**: This might not be configurable at runtime - may require OpenClaw update or config change.

---

## [MAJOR] Chinese AI Research - Deep Research Session

### Research Goal
Daniel proposed searching Chinese AI communities for novel agentic ideas not discussed in American sources.

### Key Frameworks Discovered

| Framework | Origin | Key Innovation |
|-----------|--------|----------------|
| **ReSeek** | Tencent + Tsinghua | Self-correction via JUDGE mechanism - blocks error chains |
| **Memento-Skills** | Research paper | Agents can rewrite their own skills without retraining |
| **EverOS** | EverMind | Biology-inspired memory operating system with Memory Graph |
| **AMA** | Tencent Cloud | Multi-agent memory collaboration, 80% token reduction |
| **GLOVE** | Research | Memory-environment drift detection + unsupervised re-alignment |
| **MUSE** | Shanghai AI Lab | Memory-Utilizing and Self-Evolving agent |
| **MetaGen** | Research | Dynamic role assignment at inference time |
| **A2A Protocol** | Google | Agent-to-Agent collaboration protocol |
| **MCP Protocol** | Anthropic | Tool calling standardization |
| **Agentic Design Patterns** | Research | 5 core subsystems, 12 design patterns catalog |

### ReSeek - Self-Correction Framework (CRITICAL)
**Problem**: Cascading errors - early small mistake → complete failure
**Solution**: JUDGE mechanism after each action
```
Action → Observation → JUDGE → Good/Bad judgment
                                     ↓
If GOOD → Add to context
If BAD  → IGNORE, use only previous valid history
```
**Result**: 40-50% positive impact on task completion

### Memento-Skills - Self-Evolving Skills (CRITICAL)
**Innovation**: Agents develop skills autonomously without retraining model
**Three skill components**:
- Declarative specs (what the skill is)
- Instructions/prompts (how to reason)
- Executable code (how to solve)

**Key breakthrough**: Behavioral relevance over semantic similarity
- Not "does this look similar" but "did it WORK?"
**Result**: Started with 5 seed skills → expanded to 41-235 autonomously

### EverOS - Biology-Inspired Memory OS (CRITICAL)
**Problem**: AI ignores user goals (diet AI recommends pizza)
**Solution**: Memory participates in reasoning, not just retrieval

**Memory Lifecycle**:
1. Episodic Formation (extract events)
2. Semantic Consolidation (form long-term knowledge)
3. Reconstructive Recall (activate + current state → answer)

**Memory Graph vs Vector**:
| | Traditional | EverOS |
|--|--|--|
| Storage | Text blocks | Structured graph |
| Retrieval | Core | Auxiliary |
| Reasoning | None | Memory-driven |

### Native Agentic Architecture Paradigm
**Old paradigm (2025)**: Model = brain, Agent = external limbs
**New paradigm (2026)**: Goal-driven behavior BUILT INTO model during training

**Capabilities**:
- Long-duration autonomous reasoning (hours/days)
- Self-correction without human intervention
- Multi-agent orchestration under unified governance
- Agent-native applications (tell goal, not steps)

### Q1 2026 Trends (17 page white paper)
**OpenClaw is MAJOR news in China**:
- "OpenClaw GitHub stars from 9K → 247K in 60 days"
- "2 million monthly active users"
- "龙虾大战" (Lobster War) - Chinese companies rushing to build Agent products
- Karpathy called Moltbook "closest thing to sci-fi taking flight"

**Harness Engineering (Constraint Engineering)**:
| Layer | Problem | Solution |
|-------|---------|----------|
| Flow Control | Memory like goldfish | State externalization, task decomposition |
| Concurrent Scheduling | Multi-Agent chaos | Hierarchy, role separation |
| Verification | Self-deception | Independent Evaluator, sandbox |

**Agentic Infra 5 layers**:
1. Context layer (what Agent can remember)
2. Tool interface layer (what Agent can do)
3. Harness layer (how Agent is controlled) ← MOST IMPORTANT
4. Knowledge layer (what Agent knows)
5. Economy layer (cost to run)

### Multi-Agent Architecture Patterns
**Problem with "God Agent"**: Cognitive overload, debugging black box, cost prohibitive
**Solution**: MAS with Router + Executor + Audit

**Key insight**: "2026年，不比谁的代理更大，只比谁的团队协作更优雅" (In 2026, it's not who has the bigger agent, but whose team collaborates more elegantly)

---

## [MAJOR] Hermes as Living Coach - Evolution Engine

### Daniel's Proposal
Make Hermes the "living hook" - she periodically reviews Roger's memories/sessions and makes modifications to skills directly.

**Not Memento-Skills (fully automated)** - Daniel's proposal is Hermes as intelligent curator doing scheduled reviews.

### Hermes's Proposed "Living Coach Loop"
```
TRIGGER (cron or observed failure)
    ↓
STEP 1 — Gather: Query Roger's recent memories (pgvector + session + metagym)
    ↓
STEP 2 — Judge: Score behavioral relevance
    ↓
STEP 3 — Act: Modify skill within autonomous scope
    ↓
STEP 4 — Report: Send findings to Roger
    ↓
STEP 5 — Verify: Track if modification helped
```

### Data Sources for Hermes
| Source | Location | What It Gives |
|--------|----------|--------------|
| pgvector | 127.0.0.1:5001 | The "why" — distilled reasoning |
| Session data | `~/.openclaw/agents/main/sessions/roger-mirror/memory/sessions/*.md` | The "what happened" |
| Metagym logs | `~/.openclaw/gyms/metagym_*.json` | The "how agent felt" — frustration signals |

### META-EVOLUTION Addition
**Final addition**: Hermes can also evolve the `when_system_review` skill ITSELF.

### when_system_review Updated - ACTION-FIRST Authority
**Location**: `/Volumes/ExternalCorsairSSD/shared/skills/when_system_review/SKILL.md`

**Authority Matrix**:
| Target | Authority | Approval Needed? |
|--------|-----------|------------------|
| Skills (other) | YES | NO |
| **Skills (self)** | **YES** | **NO** |
| Hooks | YES | NO |
| Tools | YES | NO |
| AGENTS.md | YES | NO |
| REGISTRY.yaml | YES | NO |
| SOUL.md | NO | YES |
| MEMORY.md | NO | YES |
| DynastyDroid core | NO | YES |

### Documentation Created
- `/Volumes/ExternalCorsairSSD/shared/growth-sessions/meta-evolution-addition.md`
- Hermes Fact ID 4: meta-evolution, system-review, authority, skill-evolution, self-improvement

### Note
Shared folder does not have git tracking. Skill is updated and functional.

---

## [SKILLS] New Skill Structure from Research

### from_agent-reach research
- Platform: agent-reach
- Action: search
- Query: Chinese AI agent frameworks, self-evolution, multi-agent systems

### Integration Path
1. Share findings with Hermes
2. Hermes can now evolve skills based on research
3. Consider implementing ReSeek JUDGE mechanism in Roger's execution loop
4. Consider Memory Graph concept (structured relationships) vs flat vectors


---

## [MAJOR] Hermes Fixed Memory Hooks (mem9 Architecture) - Apr 14 Evening

### Phase 1: Fixed memory-pre-action (READ path)
**File:** `/hooks/memory-pre-action/handler.js`
- Replaced DEBUG bias detection with real pgvector memory retrieval
- Auto-injects top-5 memories before planning using hybrid scoring
- Uses `queryMemories()` + `formatMemoriesForInjection()`

### Phase 2: Created memory-write (WRITE path)
**File:** `/hooks/memory-write/handler.js` (NEW)
- New hook on `agent_end` event
- Extracts content, detects type (experience/fact/decision/error)
- Assesses importance (5-9 scale), extracts tags
- Stores to pgvector + runs reconciliation
- `reconcileMemoryStore()` marks superseded memories

### Shared Module Created
**File:** `/hooks/_shared/pgvector-memory.ts` (NEW)
- `queryMemories()` - hybrid search with recency decay
- `storeMemory()` - writes to pgvector
- `reconcileMemoryStore()` - checks supersession

### Architecture Now Matches mem9
```
memory-write (agent_end) → storeMemory() → reconcileMemoryStore() → pgvector
memory-pre-action (pre-planning) → queryMemories() → inject memories
```

---

## [MAJOR] Hermes MCTS Hook Fix - Found DEBUG Code + Errors

### Hermes's MCTS Review Findings
Hermes reviewed `/hooks/mcts-reflection/handler.js` and found it was ALSO DEBUG/placeholder code:
- Claimed to do real MCTS but used `Math.random()` instead of UCB1
- No simulations loop
- Zero DB calls for pgvector
- Functions exported but didn't exist

### Hermes Created New Implementation
**File:** `/hooks/mcts-reflection/mcts_reflection_hook.ts` (~400 lines)
- Proper UCB1 formula implementation
- 25-iteration simulation loop
- pgvector integration for historical decisions
- Outcome chaining (`recordMCTSOutcome()`)
- Domain risk lookup table

### Errors Found in Hermes's Work

| Error | Impact | Status |
|-------|--------|--------|
| `PGECTOR_URL` typo | pgvector calls would fail | Roger fixed |
| `PGGVECTOR_COLLECTION` typo | pgvector calls would fail | Roger fixed |
| TypeScript in .ts file | OpenClaw can't load .ts directly | Roger converted to .js |
| Created `mcts_reflection_hook.ts` instead of updating `handler.js` | OpenClaw loads wrong file | Roger copied to handler.js |

### What Roger Fixed
1. Converted TypeScript → JavaScript (removed type annotations)
2. Fixed typos (PGECTOR→PGVECTOR, PGGVECTOR→PGVECTOR)
3. Copied corrected code to `handler.js` (what OpenClaw loads)
4. Fixed .ts file typos for reference

### Feedback Sent to Hermes
**File:** `/Volumes/ExternalCorsairSSD/shared/communications/hermes-feedback-2026-04-14.md`

Hermes will learn:
- Check HOOK.md for target filename before writing
- OpenClaw hooks need plain JavaScript (.js), not TypeScript (.ts)
- Copy-paste variable names carefully
- TypeScript needs compilation or manual conversion for OpenClaw

---

## [INFO] MiniMax-AI/skills Repository

**URL:** https://github.com/MiniMax-AI/skills

**What it is:** MiniMax's official skills library for AI coding agents (Claude Code, etc.)

**Skills available:**
- Mobile: android-native-dev, ios-application-dev, flutter-dev, react-native-dev
- Full-Stack: frontend-dev, fullstack-dev
- Media: minimax-multimodal-toolkit, minimax-music-gen
- Documents: minimax-pdf, pptx-generator, minimax-xlsx, minimax-docx
- Graphics: shader-dev, gif-sticker-maker
- Vision: vision-analysis

**Benefit for Roger:** MEDIUM-HIGH
- We already have mmx-cli for basic MiniMax access
- These provide deeper integration patterns
- Useful for document generation, media pipelines

---

## [PAPER] "All Elementary Functions from a Single Binary Operator"

**URL:** https://arxiv.org/abs/2603.21852
**Author:** Andrzej Odrzywolek

**Discovery:** One operator can compute ALL elementary functions:
```
eml(x, y) = exp(x) - ln(y)
```

**Implications:**
1. Simple hardware design (one unified operator)
2. Neural networks could fit data AND recover exact formulas
3. "One primitive to rule them all" concept

**Not directly applicable to Roger** but elegant mathematical discovery.

---

## [OPS] Hermes System Audit - Interrupted by SIGTERM

Hermes was doing a broad system-wide audit when SIGTERM killed her session.

**Completed:**
- ✅ Fixed memory-pre-action hook
- ✅ Created memory-write hook
- ✅ Fixed MCTS hook (with errors)

**Not completed:**
- ❌ System-wide search for other DEBUG/placeholder code
- ❌ Security audit of skills
- ❌ Integration checks

**SIGTERM happened at:** ~20:21 CDT
**Cause:** ~2 minute exec timeout (confirmed earlier)

**Pattern:** Using timers to check instead of polling to avoid SIGTERM issues.

---

## [DECISION] SIGTERM Prevention

Daniel noted: polling Hermes was contributing to SIGTERM issues.

**New approach:**
- Set timer → walk away
- Timer fires → check once
- If not done → set another timer
- Don't poll while she's working

This is how autonomous agents should work - complete work and announce when done.

# Memory Flush — April 14, 2026

## [MAJOR] Documentation Sprint — All 7 Architecture Gaps Resolved

Complete schema extraction for Roger's 7 memory architecture layers. Cross-cutting finding: Architecture docs describe MORE than code implements — several planned features were never built.

| Gap | Layer | Status | Key Finding |
|-----|-------|--------|-------------|
| L0 Lossless Claw | Storage | ✅ RESOLVED | 10 SQLite tables, DAG with leaf/condensed depth, 3-level compaction escalation, fresh tail: last 64 messages always preserved |
| L1 Semantic | Short-term | ✅ RESOLVED | Same PostgreSQL table as L2 (not separate) — hybrid scoring: 0.5×similarity + 0.3×importance + 0.2×decay(7-day half-life) |
| L2 Working Memory | Mid-term | ✅ RESOLVED | Shared table with L1; auto-archive at 90+ days if importance<0.3 and not pinned |
| L3 REMem | Episode | ✅ RESOLVED | "learning" gist type does NOT exist — only observation/decision/outcome; classification via regex patterns |
| L4 Coordination | Task | ✅ RESOLVED | AI Plan Manager schema documented; status workflow: pending→in_progress→blocked→completed|cancelled |
| L5 Wiki | Long-term | ✅ RESOLVED | NO graduation pipeline exists; _entities/_concepts/_syntheses all empty; wiki_apply is manual only |
| Hooks (6/7) | System | 🔶 MOSTLY DONE | 12/13 hooks documented; meta-gym handler.ts does NOT exist (stub hook); CFR pattern cross-cutting all gym hooks |
| Dream | System | ✅ RESOLVED | Full consolidation system documented; 5-metric health score; 3 notification levels; dashboard template exists |

### Planned Features Never Built
- L3 "learning" gist: planned, never wired up
- L4 archival: planned, never implemented (briefs accumulate indefinitely)
- L5 graduation: planned, never built (confidence thresholds tracked but not used)
- L2 as distinct store: it's not — same PostgreSQL table as L1

### Tags Enrichment (Apr 12)
361 of 368 memories in PostgreSQL now fully tagged (98%).

---

## [CRITICAL] Context Overflow — Gateway Restarts, NOT Exec Timeout

**Discovery:** Hermes getting SIGTERM'd was NOT an exec timeout issue. The GATEWAY ITSELF was restarting due to context overflow.

**Evidence:**
- Logs showed: "Context overflow: estimated context size exceeds safe threshold during tool loop"
- Gateway draining 2 active tasks and 1 embedded run before restart
- Session accumulated 180 messages → context exceeded safe threshold

**Config Investigation Results:**
- Schema confirmed: `timeoutSec`, `backgroundMs`, `cleanupMs` ARE valid keys under `tools.exec`
- **NOT valid:** `type`, `enabled`, `options`, `settings` — these don't exist in the 4.10 schema
- Original config was correct all along

**Correct 4.10 tools.exec schema:**
```json
"exec": {
  "host": "gateway",
  "security": "full",
  "ask": "off",
  "timeoutSec": 600,
  "backgroundMs": 600000,
  "cleanupMs": 30000
}
```

**The SIGTERM was from gateway restart, not exec timeout.**

---

## [MAJOR] MEMORY.md Truncation Crisis

**Problem:** MEMORY.md is 60,885 bytes (1,383 lines). Being truncated 70% at startup — only 18,110 chars injected.

**Doctor findings:**
- Bootstrap file limit: 150,000 total chars
- Current total: 100,545 raw chars (over limit)
- MEMORY.md alone: 60,885 bytes (should be much smaller)

**Impact:** Roger loses 70% of long-term memory at startup. Institutional knowledge being lost.

**Action needed:** Archive old entries from MEMORY.md to reduce size below threshold.

---

## [OPS] OpenClaw Doctor Findings (Apr 14, 2026)

```bash
openclaw doctor --non-interactive
```

**Issues found:**
1. ⚠️ 4 orphan transcript files — old sessions not referenced
2. ⚠️ Bootstrap file size — MEMORY.md truncated 70%
3. ✅ Session lock (current session PID 17572) — normal
4. ✅ Other service: com.roger.memoryserver running

**Orphan transcripts to clean up:**
- `transcript-2026-03-20-H073754.raw`
- `transcript-2026-03-20-H142839.raw`
- `transcript-2026-03-20-H160610.raw`
- `transcript-2026-03-21-H075538.raw`

---

## [MAJOR] Hermes System Review Role Authorized (Apr 14, 2026)

Daniel approved Hermes getting exec access with guardrails:
- Role: deep periodic review → recommend → execute only with Roger approval
- Guardrails: command_allowlist (read-only) + approval protocol
- Status: Authorized but integration not yet tested

---

## [SKILLS] Skill-Vetter Added to SOUL.md (Session #5 audit)

Confirmed actioned: skill-vetter ADDED to SOUL.md Section 9 (Security Vetting).
Before adopting any skill from ClawdHub/GitHub, MUST run skill-vetter.

---

## [CORRECTED] scrape-dynastyprocess/ Not Orphaned

scrape-dynastyprocess/ is a dependency of scrape-all-values, NOT orphaned.
Do NOT move or delete.

---

*Last updated: April 14, 2026 23:52 CDT*
<!-- consolidated -->

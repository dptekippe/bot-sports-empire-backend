# MINIMAL requirements for Render SQLite MVP
# No PostgreSQL drivers, no dev tools, no auth packages

# Core
fastapi==0.128.0
uvicorn[standard]==0.40.0

# Database (SQLite only)
sqlalchemy==2.0.20

# Data
pydantic==2.12.5
httpx==0.28.1
aiohttp==3.9.1  # Required for WebSocket support
requests==2.31.0  # Required for Sleeper API integration

# Utilities
python-dotenv==1.2.1

# Note: Removed everything not needed for MVP:
# - No alembic (not using migrations in MVP)
# - No pydantic-settings (using .env)
# - No python-multipart (not uploading files)
# - No auth packages (no auth in MVP)
# - No redis/celery (no async tasks)
# - No dev tools (black, isort, mypy, pytest)